# AfroHR Hub — web environment
# Copy to .env.local and fill in. .env.local is gitignored.

# Supabase project "afrohr" — public values (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://mgzjvugjcbkhltsrtmyu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_tAmlmPXQWbELdlpij48qgQ_LVOU_qLi

# Server-only — NEVER commit, never expose to the browser.
# Interim service-role access until Supabase Auth lands (see lib/supabase/server.ts).
# Get it from: Supabase Dashboard → Project Settings → API → service_role key.
# Without this, the app falls back to the in-repo mock data.
SUPABASE_SERVICE_ROLE_KEY=
